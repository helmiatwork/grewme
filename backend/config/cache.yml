default: &default
  store_options:
    max_size: <%= 256.megabytes %>
    namespace: <%= Rails.env %>
    max_age: <%= 1.day.to_i %>
    expiry_batch_size: 100

development:
  <<: *default

test:
  <<: *default

production:
  database: cache
  <<: *default
