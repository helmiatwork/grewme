---
phase: 01-security-foundation-backend-core
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/test/fixtures/users.yml
  - backend/test/fixtures/schools.yml
  - backend/test/fixtures/classrooms.yml
  - backend/test/fixtures/students.yml
  - backend/test/fixtures/daily_scores.yml
  - backend/test/fixtures/parent_students.yml
  - backend/test/test_helper.rb
  - backend/Gemfile
autonomous: true
requirements: [TEST-05, TEST-06]

must_haves:
  truths:
    - "All fixtures load without database constraint violations"
    - "SimpleCov is integrated and reports coverage when tests run"
    - "Auth test helpers are available for all integration tests"
  artifacts:
    - path: "backend/test/fixtures/users.yml"
      provides: "Valid user fixtures with teacher, parent, admin roles and bcrypt password digests"
      contains: "password_digest: <%= BCrypt::Password.create"
    - path: "backend/test/fixtures/classrooms.yml"
      provides: "Classroom fixtures with proper school/teacher associations"
    - path: "backend/test/test_helper.rb"
      provides: "Test helper with SimpleCov, AuthTestHelper module"
      contains: "SimpleCov"
    - path: "backend/Gemfile"
      provides: "SimpleCov gem in test group"
      contains: "simplecov"
  key_links:
    - from: "backend/test/test_helper.rb"
      to: "backend/test/fixtures/*.yml"
      via: "fixtures :all"
      pattern: "fixtures :all"
    - from: "backend/test/test_helper.rb"
      to: "backend/app/services/jwt_service.rb"
      via: "AuthTestHelper generates JWT tokens"
      pattern: "JwtService\\.encode"
---

<objective>
Build the test infrastructure that all subsequent test plans depend on: fix broken fixtures with realistic data, add SimpleCov for coverage reporting, and create auth test helpers for integration tests.

Purpose: Without valid fixtures and test helpers, no tests can run. This unblocks all testing plans.
Output: Working fixtures, SimpleCov integration, AuthTestHelper module.
</objective>

<execution_context>
@/Users/theresiaputri/.config/Claude/get-shit-done/workflows/execute-plan.md
@/Users/theresiaputri/.config/Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/rails-api-patterns.md

<interfaces>
<!-- Current broken fixtures pattern -->

From backend/test/fixtures/users.yml (BROKEN):
```yaml
one:
  name: MyString
  email: MyString
  password_digest: MyString
  role: 1
two:
  name: MyString
  email: MyString
  password_digest: MyString
  role: 1
```

From backend/test/test_helper.rb:
```ruby
ENV["RAILS_ENV"] ||= "test"
require_relative "../config/environment"
require "rails/test_help"

module ActiveSupport
  class TestCase
    fixtures :all
    parallelize(workers: :number_of_processors)
  end
end
```

From backend/app/models/user.rb:
```ruby
enum :role, { teacher: 0, parent: 1, admin: 2 }
```

From backend/db/schema.rb (relevant tables):
- users: name, email, password_digest, role(integer)
- schools: name
- classrooms: name, school_id(FK), teacher_id(FK)
- students: name, classroom_id(FK), avatar
- parent_students: parent_id(FK), student_id(FK) — unique on (parent_id, student_id)
- daily_scores: student_id(FK), teacher_id(FK), date, skill_category(integer), score(integer), notes — unique on (student_id, date, skill_category)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix all fixtures with valid, realistic data</name>
  <files>backend/test/fixtures/users.yml, backend/test/fixtures/schools.yml, backend/test/fixtures/classrooms.yml, backend/test/fixtures/students.yml, backend/test/fixtures/daily_scores.yml, backend/test/fixtures/parent_students.yml</files>
  <action>
Replace ALL fixture files with valid, referentially consistent data. Use the research-recommended fixture patterns.

**users.yml** — 4 users covering all roles:
```yaml
teacher_alice:
  name: Alice Teacher
  email: alice@school.test
  password_digest: <%= BCrypt::Password.create("password123") %>
  role: 0  # teacher

teacher_bob:
  name: Bob Teacher
  email: bob@school.test
  password_digest: <%= BCrypt::Password.create("password123") %>
  role: 0  # teacher

parent_carol:
  name: Carol Parent
  email: carol@parent.test
  password_digest: <%= BCrypt::Password.create("password123") %>
  role: 1  # parent

admin_dave:
  name: Dave Admin
  email: dave@admin.test
  password_digest: <%= BCrypt::Password.create("password123") %>
  role: 2  # admin
```

**schools.yml** — 1 school:
```yaml
greenwood:
  name: Greenwood Elementary
```

**classrooms.yml** — 2 classrooms (one per teacher):
```yaml
alice_class:
  name: "Class 1A"
  school: greenwood
  teacher: teacher_alice

bob_class:
  name: "Class 2B"
  school: greenwood
  teacher: teacher_bob
```

**students.yml** — 3 students across 2 classrooms:
```yaml
student_emma:
  name: Emma
  classroom: alice_class
  avatar: ""

student_finn:
  name: Finn
  classroom: alice_class
  avatar: ""

student_grace:
  name: Grace
  classroom: bob_class
  avatar: ""
```

**parent_students.yml** — link parent_carol to student_emma:
```yaml
carol_emma:
  parent: parent_carol
  student: student_emma
```

**daily_scores.yml** — sample scores covering different skills and dates:
```yaml
emma_reading_day1:
  student: student_emma
  teacher: teacher_alice
  date: 2026-03-01
  skill_category: 0
  score: 85
  notes: "Good reading progress"

emma_math_day1:
  student: student_emma
  teacher: teacher_alice
  date: 2026-03-01
  skill_category: 1
  score: 72
  notes: ""

grace_reading_day1:
  student: student_grace
  teacher: teacher_bob
  date: 2026-03-01
  skill_category: 0
  score: 90
  notes: ""
```

Ensure: no duplicate emails, valid bcrypt digests, all FKs reference existing fixtures, unique constraint on (student_id, date, skill_category) respected.
  </action>
  <verify>
    <automated>cd backend && bin/rails db:test:prepare 2>&1 | tail -5 && echo "FIXTURES_VALID"</automated>
  </verify>
  <done>All 6 fixture files contain valid, realistic data. Fixtures load without constraint violations. Covers teacher/parent/admin roles, 2 classrooms, 3 students, cross-teacher authorization testing scenario.</done>
</task>

<task type="auto">
  <name>Task 2: Add SimpleCov and AuthTestHelper to test infrastructure</name>
  <files>backend/Gemfile, backend/test/test_helper.rb</files>
  <action>
1. **Add SimpleCov to Gemfile** in the test group:
   ```ruby
   group :test do
     gem "simplecov", require: false
   end
   ```
   Run `bundle install` after editing.

2. **Update test_helper.rb** — SimpleCov MUST be loaded FIRST before any app code:
   ```ruby
   require "simplecov"
   SimpleCov.start "rails" do
     enable_coverage :branch
     minimum_coverage 50
     add_filter "/test/"
     add_filter "/config/"
     add_filter "/db/"
   end

   ENV["RAILS_ENV"] ||= "test"
   require_relative "../config/environment"
   require "rails/test_help"

   module AuthTestHelper
     def auth_headers(user)
       token = JwtService.encode(user_id: user.id)
       { "Authorization" => "Bearer #{token}", "Content-Type" => "application/json" }
     end

     def auth_get(path, user:, params: {})
       get path, params: params, headers: auth_headers(user)
     end

     def auth_post(path, user:, params: {})
       post path, params: params.to_json, headers: auth_headers(user)
     end

     def auth_put(path, user:, params: {})
       put path, params: params.to_json, headers: auth_headers(user)
     end

     def auth_delete(path, user:)
       delete path, headers: auth_headers(user)
     end
   end

   module ActiveSupport
     class TestCase
       fixtures :all
       parallelize(workers: :number_of_processors)
     end
   end

   class ActionDispatch::IntegrationTest
     include AuthTestHelper
   end
   ```

   Key points:
   - `SimpleCov.start "rails"` uses the Rails profile (auto-groups by models/controllers/etc.)
   - `enable_coverage :branch` tracks branch coverage too
   - `minimum_coverage 50` enforces the Phase 1 target
   - `AuthTestHelper` provides convenience methods for authenticated API calls
   - Include AuthTestHelper in `ActionDispatch::IntegrationTest` so all controller tests get it
  </action>
  <verify>
    <automated>cd backend && bundle install --quiet && bin/rails test 2>&1 | tail -10</automated>
  </verify>
  <done>SimpleCov runs on every test execution and reports coverage. AuthTestHelper is available in all integration tests. Minimum coverage threshold set to 50%.</done>
</task>

</tasks>

<verification>
1. `cd backend && bin/rails db:test:prepare` succeeds (fixtures load)
2. `cd backend && bin/rails test` runs without fixture errors
3. `grep "simplecov" backend/Gemfile` returns a match
4. `grep "SimpleCov" backend/test/test_helper.rb` returns a match
5. `grep "AuthTestHelper" backend/test/test_helper.rb` returns a match
6. Coverage report generated in `backend/coverage/` after test run
</verification>

<success_criteria>
- All fixtures are valid with realistic data — no MyString placeholders remain
- Fixtures load without database constraint violations
- SimpleCov integrated, reports coverage on test runs
- AuthTestHelper module provides auth_get, auth_post, auth_put, auth_delete helpers
- Coverage minimum threshold set to 50%
</success_criteria>

<output>
After completion, create `.planning/phases/01-security-foundation-backend-core/01-02-SUMMARY.md`
</output>
